_target_: experiments.concept_bottleneck.sae.sae_module.plSAE
sae_type: "topk"
loss_type: "mse_auxk"
optimizer_type: "adam"
lr: ${sae_lr}
num_samples: 20000
resample_every_n_epochs: 100 # 100 Equivalent to NO resampling or 1 for resampling at each epoch
resample_batch_size: 32
bind_init: True
label_path: "${paths.dict_path}/pke_yake_general_f1k2n5max1500000parserFalse_homogeneous_30000_centroids_label.csv"
text_enc_kwargs:
    model_name: Llama3-MS-CLIP-Base
    pretrained: True
    channels: 10
    ckpt_path: null
text_batch_size: 128
# name_class: {0: "background", 1: "TC",  2: "AR"}

sae_kwargs:
  input_shape: ${input_shape}
  nb_concepts : ${nb_concepts}
  top_k: ${nb_k}
  device: "cuda"

criterion_kwargs:
  penalty: 0.1

arch_kwargs:
  points_from: "vocab"
  csv_path: null #"${paths.dict_path}/pke_yake_general_f1k2n5max1500000parserFalse_homogeneous.csv"
  npy_path: "${paths.dict_path}/pke_yake_general_f1k2n5max1500000parserFalse_homogeneous_30000_centroids.npy" #"${paths.dict_path}/spacy_msclip_general_f1k2n5max1500000parserFalse_homogeneous_30000_centroids.npy"
  delta: 0.5
  use_multiplier: True
